using System;
using UnityEngine;

namespace Asteroid
{
    internal static class FactoryBullet
    {
        //public static Rigidbody2D CreateBullet(float demage,GameObject bullet)
        //{

        //    if (!bullet.TryGetComponent<Bullet>(out Bullet _bullet))
        //    {
        //        var s = bullet.AddComponent<Bullet>();
        //        s.demage = this.demage;
        //        s.Hit += () => pool.Push(bullet);
        //    }
        //    return bullet.GetComponent<Rigidbody2D>();
        //}
    }
}
